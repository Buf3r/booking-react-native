import { View, Text, Pressable } from 'react-native'
import React from 'react'

const Pick = ({item}) => {
    <Pressable
        onPress={()=>}
    ></Pressable>
  return (
    <View>
      <Text>Pick</Text>
    </View>
  )
}

export default Pick